# Litigant Portal - Development Environment Variables
# Copy to .env for local Docker development: cp .env.example .env
#
# For production, use Docker secrets instead (see secrets/README.md)

# Django Settings
# ----------------
# Leave empty for dev (auto-generated), set for local non-Docker dev
SECRET_KEY=""

# Set to false for production testing
DEBUG=true

# PostgreSQL (dev only - prod uses Docker secrets)
# ------------------------------------------------
POSTGRES_USER=""
POSTGRES_PASSWORD=""
POSTGRES_DB=""

# Production Settings (optional overrides)
# ----------------------------------------
# ALLOWED_HOSTS=your-domain.com
# GUNICORN_WORKERS=3
# GUNICORN_THREADS=2
