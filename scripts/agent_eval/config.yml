slug: initial-baseline
systems: [raw, old, new]
models: [luna, terra]
model_ids:
  luna: bedrock_mantle/openai.gpt-5.6-luna
  terra: bedrock_mantle/openai.gpt-5.6-terra
  sol: bedrock_mantle/openai.gpt-5.6-sol
repetitions: 3
judge_model: sol
# Temporarily use this alias for the old agent's title calls; null preserves
# the site's current setting. The original setting is restored after the run.
legacy_fast_model: luna
weights:
  facts: 0.70
  qualitative: 0.30
# Omit cases to run the whole suite, or list IDs from cases.yml.
timeout_seconds: 300
seed: 179
# Optional USD rates per million tokens, keyed by full model identifier.
# Each override needs input_per_million, output_per_million, and source.
# If cached tokens occur, cached_input_per_million is also required.
prices: {}
