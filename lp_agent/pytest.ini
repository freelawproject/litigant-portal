[pytest]
pythonpath = ..
testpaths = tests
python_files = test_*.py
python_classes = *Tests
norecursedirs = providers
addopts = -ra --strict-markers
markers =
    postgres: requires the local database exercise in var/scripts
