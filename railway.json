{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "web-prod",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
